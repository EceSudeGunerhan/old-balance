/*

var mongoose = require("mongoose");

// const Venue = mongoose.model("venue");
// const Comment = mongoose.model("comment");

function createResponse(res, status, content) {
    res.status(status);
    res.json(content);
}

function commentsAddOne(req, res) {
    createResponse(res, 200, { "status": "success" });
}

function commentsReadOne(req, res) {
    createResponse(res, 200, { "status": "success" });
}

function commentsUpdateOne(req, res) {
    createResponse(res, 200, { "status": "success" });
}

function commentsDeleteOne(req, res) {
    createResponse(res, 200, { "status": "success" });
}

module.exports = {
    commentsAddOne,
    commentsReadOne,
    commentsUpdateOne,
    commentsDeleteOne
}

*/